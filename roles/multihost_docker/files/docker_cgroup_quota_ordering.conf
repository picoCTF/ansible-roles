# Ensures the container slice ceilings are applied before dockerd can place any
# container into the slice, including on a manual `systemctl restart docker`.
[Unit]
Wants=docker_cgroup_quota.service
After=docker_cgroup_quota.service
