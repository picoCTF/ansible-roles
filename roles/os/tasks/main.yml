---
- { include: upgrade.yml, become: yes, when: upgrade_packages }
- { include: journald.yml, become: yes }
- { include: sysctls.yml, become: yes }
- { include: hostname.yml, become: yes, when: hostname }
