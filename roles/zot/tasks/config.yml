---

- name: Ensure zot config file exists
  ansible.builtin.template:
    src: "config.json.j2"
    dest: "{{ zot_config_dir }}/config.json"
    lstrip_blocks: yes
    force: yes
  notify:
    - Restart zot
