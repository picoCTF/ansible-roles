# Changelog

Notable changes to the `picoctf.ansible_roles` collection, newest first.

An entry is added when `version` in `galaxy.yml` is raised. For anything not
recorded here, the git history is the authority; this file is a summary for
people consuming the collection, not a replacement for it.

## 1.0.0

The current version, which predates this file. What went into it is described
by the repository history rather than summarised here.
